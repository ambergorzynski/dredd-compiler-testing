#!/bin/sh

# This file returns 0 if the reduced program is 'interesting'

# Recondition the reduced program

# Extract inputs

# Execute the reconditioned program with inputs

# Check that ouptuts still differ from original outputs (stored somewhere)

# If outputs are valid but differ (i.e. no crashes!), return 0

# Otherwise (same output or crash), return 1
